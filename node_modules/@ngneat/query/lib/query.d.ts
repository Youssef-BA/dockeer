import { Injector } from '@angular/core';
import { DefinedQueryObserverResult, QueryKey, QueryObserverResult } from '@tanstack/query-core';
import { Result } from './types';
import { DefinedInitialDataOptions, UndefinedInitialDataOptions } from './query-options';
/**
 *
 * Optionally pass an injector that will be used than the current one.
 * Can be useful if you want to use it in ngOnInit hook for example.
 *
 * @example
 *
 * injector = inject(Injector);
 *
 * ngOnInit() {
 *  const todos = getTodos({ injector: this.injector }).result;
 * }
 *
 */
export declare function injectQuery(options?: {
    injector?: Injector;
}): {
    <TQueryFnData = unknown, TError = Error, TData = TQueryFnData, TQueryKey extends QueryKey = QueryKey>(options: UndefinedInitialDataOptions<TQueryFnData, TError, TData, TQueryKey>): Result<QueryObserverResult<TData, TError>>;
    <TQueryFnData_1 = unknown, TError_1 = Error, TData_1 = TQueryFnData_1, TQueryKey_1 extends QueryKey = QueryKey>(options: DefinedInitialDataOptions<TQueryFnData_1, TError_1, TData_1, TQueryKey_1>): Result<DefinedQueryObserverResult<TData_1, TError_1>>;
};
